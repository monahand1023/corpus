# Required for ingest + query (Voyage embeddings).
# Sign up: https://dash.voyageai.com/
VOYAGE_API_KEY=

# Optional. Required ONLY if you run `corpus-summarize` to pre-generate
# per-doc summaries via Claude Haiku.
# Sign up: https://console.anthropic.com/
ANTHROPIC_API_KEY=
